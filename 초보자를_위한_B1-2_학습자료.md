# 초보자를 위한 B1-2 학습 자료

## 이 자료의 목표

이 자료는 Linux와 프로그래밍을 잘 모르는 사람도 B1-2 문제와 결과물을 이해하고, 다른 사람에게 자기 말로 설명할 수 있도록 만든 입문 교재다.

학습을 마치면 다음 질문에 답할 수 있어야 한다.

1. 프로그램과 프로세스는 무엇이 다른가?
2. CPU와 메모리는 각각 어떤 일을 하는가?
3. OOM, CPU Spike, Deadlock은 어떻게 다른가?
4. 프로그램이 종료됐을 때 왜 로그부터 확인해야 하는가?
5. 관제 결과에서 어떤 숫자와 문장을 증거로 선택해야 하는가?
6. 환경변수를 바꾼 전후를 왜 비교해야 하는가?
7. 이 프로젝트의 문서와 스크립트는 각각 무슨 역할을 하는가?

이 자료는 앞에서부터 순서대로 읽는 것을 권장한다. 명령어를 모두 외울 필요는 없다. 먼저 상황과 원리를 이해한 뒤, 명령어가 어떤 질문에 답하는 도구인지 익히면 된다.

---

## 1단계. 이번 문제를 일상 언어로 바꾸기

### 문제 상황

어떤 회사의 서버에서 프로그램이 갑자기 느려지거나 멈췄다고 생각해 보자. 당황해서 서버를 바로 재부팅하면 서비스는 잠시 살아날 수 있다. 하지만 왜 멈췄는지 보여주는 현장 증거가 사라질 수 있다. 원인을 고치지 않았으므로 같은 장애가 다시 발생할 가능성도 높다.

이번 문제에서 해야 할 일은 다음과 같다.

```text
장애 발생
   ↓
프로세스와 자원 상태 관찰
   ↓
로그와 명령어 출력을 증거로 수집
   ↓
원인 추론
   ↓
환경 설정을 바꾸어 임시 조치
   ↓
변경 전과 변경 후 비교
   ↓
다른 개발자가 이해할 수 있는 보고서 작성
```

### 세 가지 장애

| 장애 | 쉬운 표현 | 이 프로젝트에서 보이는 결과 |
| --- | --- | --- |
| OOM/Memory Leak | 물건을 계속 쌓아 창고가 가득 참 | 메모리 증가 후 강제 종료 |
| CPU Spike | 한 사람이 계산기를 독점함 | CPU 부하 증가 후 보호 종료 |
| Deadlock | 두 사람이 서로 가진 열쇠를 기다림 | 프로세스는 살았지만 작업 정지 |

핵심은 세 장애의 겉모습이 비슷할 수 있다는 점이다. 사용자는 모두 “프로그램이 안 된다”고 말할 수 있지만, 실제 원인과 해결 방법은 전혀 다르다.

---

## 2단계. 가장 먼저 알아야 할 컴퓨터 용어

### 2.1 운영체제

운영체제는 컴퓨터의 자원을 관리하는 총관리자다. Windows, macOS, Linux가 운영체제에 해당한다.

운영체제는 프로그램에 다음 자원을 나누어 준다.

- CPU를 사용할 시간
- 데이터를 둘 메모리 공간
- 파일을 읽고 쓸 권한
- 네트워크 포트
- 사용자와 그룹별 권한

이번 실습에서는 Linux 운영체제 위에서 프로그램이 자원을 어떻게 사용하는지 관찰한다.

### 2.2 프로그램과 프로세스

프로그램은 디스크에 저장된 실행 파일이다. 아직 실행하지 않은 요리법과 비슷하다.

프로세스는 그 프로그램을 실제로 실행한 상태다. 요리법을 보고 주방에서 실제 요리를 시작한 것과 같다.

```text
agent-leak-app-arm64  = 저장된 프로그램 파일
PID 2970              = 실행 중인 프로세스의 식별 번호
```

같은 프로그램을 두 번 실행하면 파일은 하나지만 프로세스와 PID는 두 개가 될 수 있다.

### 2.3 PID

PID는 Process ID의 줄임말이다. 운영체제가 실행 중인 프로세스를 구분하기 위해 붙이는 번호다.

사람을 이름만으로 구분하기 어렵기 때문에 주민등록번호나 사번을 사용하는 것과 비슷하다. `kill`, `ps` 같은 명령은 PID를 이용해 정확한 프로세스를 조회하거나 종료한다.

### 2.4 CPU

CPU는 명령을 계산하고 실행하는 일꾼이다.

- CPU 사용률이 낮다: 계산 작업이 적거나 다른 사건을 기다리고 있다.
- CPU 사용률이 높다: 계산을 많이 하고 있다.
- 한 프로세스가 CPU를 오래 독점한다: 다른 작업의 응답이 늦어질 수 있다.

CPU 사용률이 높다고 항상 장애는 아니다. 영상 변환이나 과학 계산처럼 원래 계산량이 큰 작업일 수도 있다. 중요한 것은 “정상 업무 때문에 높은가, 잘못된 반복 때문에 높은가”를 로그와 서비스 지연으로 함께 판단하는 것이다.

### 2.5 메모리

메모리는 실행 중인 프로그램이 당장 사용할 데이터를 올려 두는 작업대다.

책상 위에 필요한 서류를 펼쳐 놓는 상황을 생각하면 쉽다. 일을 끝낸 서류를 계속 치우지 않으면 작업대가 가득 찬다. 프로그램도 더 이상 필요하지 않은 데이터를 계속 붙잡고 있으면 사용 가능한 메모리가 줄어든다.

### 2.6 RSS와 Heap

이번 결과물에는 RSS와 Heap이라는 값이 나온다.

- Heap: 프로그램이 실행 중에 동적으로 만든 데이터를 두는 영역이다.
- RSS: 현재 프로세스가 실제 물리 메모리에 올려 둔 전체 크기에 가까운 운영체제 관측값이다.

둘은 측정 범위가 다르므로 숫자가 정확히 같을 필요는 없다. 중요한 것은 두 값이 시간에 따라 같은 방향으로 계속 증가하는지다.

실제 OOM 테스트에서는 다음 변화가 관측됐다.

```text
내부 Heap: 25MB → 50MB
외부 RSS : 16.8MB → 41.8MB
```

서로 다른 측정 방식이 모두 증가를 가리키므로 메모리 증가 주장의 신뢰도가 높아진다.

### 2.7 스레드

하나의 프로세스 안에서도 여러 작업을 동시에 진행할 수 있다. 이때 작업 흐름 하나를 스레드라고 한다.

식당으로 비유하면 다음과 같다.

- 프로세스: 식당 하나
- 스레드: 식당 안에서 일하는 직원들
- 공유 메모리: 직원들이 함께 사용하는 주방과 냉장고

직원이 많으면 여러 주문을 빠르게 처리할 수 있지만, 같은 도구를 동시에 사용하려 하면 충돌이 생길 수 있다.

### 2.8 락

락은 공유 자원을 한 번에 한 스레드만 사용하게 하는 잠금장치다.

화장실 열쇠가 하나뿐인 카페를 생각해 보자. 열쇠를 가진 사람만 화장실을 사용할 수 있으므로 충돌은 막을 수 있다. 하지만 여러 열쇠를 잘못된 순서로 기다리면 모두가 움직이지 못하는 문제가 생길 수 있다.

### 2.9 포트

포트는 네트워크 프로그램이 사용하는 논리적인 출입구 번호다. 이 앱은 TCP 15034번 포트를 사용한다.

```text
PORT:15034:LISTEN
```

위 출력은 프로그램이 15034번 출입구에서 연결을 기다리고 있다는 뜻이다. 포트가 열려 있다는 사실만으로 프로그램의 내부 작업까지 정상이라고 단정할 수는 없다. Deadlock에서는 포트와 PID가 살아 있는데도 실제 작업이 멈춘다.

---

## 3단계. Linux 명령어를 “질문”으로 이해하기

명령어를 외우기보다 각 명령이 컴퓨터에 어떤 질문을 하는지 이해하자.

| 명령 | 컴퓨터에 묻는 질문 |
| --- | --- |
| `uname -m` | 이 컴퓨터의 CPU 구조는 무엇인가? |
| `file 파일명` | 이 파일은 어떤 운영체제와 CPU용인가? |
| `id` | 나는 어떤 사용자와 그룹인가? |
| `pgrep -af 이름` | 이 이름과 관련된 프로세스가 실행 중인가? |
| `ps` | 프로세스가 CPU와 메모리를 얼마나 쓰는가? |
| `ps -L` | 프로세스 안의 스레드들은 어떤 상태인가? |
| `ss -ltnp` | 어떤 TCP 포트가 연결을 기다리고 있는가? |
| `tail -f 로그` | 새 로그가 추가되는 모습을 계속 보여 달라. |
| `kill PID` | 해당 PID에 종료 요청을 보내라. |

### 명령 출력은 답안이 아니라 증거다

예를 들어 `pgrep`에서 PID가 보인다는 사실은 “프로세스가 존재한다”는 것만 증명한다. 서비스가 정상 응답한다는 사실까지 증명하지 않는다.

좋은 장애 분석은 여러 질문의 답을 조합한다.

```text
PID가 있는가?             → pgrep
CPU와 메모리가 변하는가? → ps, monitor.sh
스레드가 어디서 쉬는가?  → ps -L
포트가 열려 있는가?      → ss
마지막 행동은 무엇인가?  → 애플리케이션 로그
```

---

## 4단계. 환경변수 이해하기

환경변수는 프로그램을 다시 만들지 않고 실행 방식을 바꾸는 외부 설정값이다.

에어컨의 내부 부품을 바꾸지 않고 리모컨으로 목표 온도를 설정하는 것과 비슷하다.

### 이 앱에 필요한 주요 환경변수

| 변수 | 의미 | 실습값 |
| --- | --- | --- |
| `AGENT_HOME` | 앱의 기준 디렉터리 | `.agent-home` 또는 `/tmp/b1-2-agent` |
| `AGENT_PORT` | 네트워크 포트 | 15034 |
| `AGENT_UPLOAD_DIR` | 업로드 파일 경로 | `$AGENT_HOME/upload_files` |
| `AGENT_KEY_PATH` | API 키 디렉터리 | `$AGENT_HOME/api_keys` |
| `AGENT_LOG_DIR` | 로그 저장 경로 | `$AGENT_HOME/logs` |
| `MEMORY_LIMIT` | 메모리 보호 제한 | 50~512MB |
| `CPU_MAX_OCCUPY` | CPU 부하 설정 상한 | 10~100% |
| `MULTI_THREAD_ENABLE` | 동시 스레드 실행 여부 | true/false |

### 부트 검사란 무엇인가

프로그램은 본 작업을 시작하기 전에 필요한 조건을 확인한다.

```text
[1/6] 사용자 확인
[2/6] 환경변수 확인
[3/6] secret.key 확인
[4/6] 포트 사용 가능 여부 확인
[5/6] 로그 쓰기 권한 확인
[6/6] 장애 실습 설정값 확인
```

`AGENT_HOME` 없이 실행했을 때 두 번째 검사에서 실패했다. 이 상태에서는 장애 실험을 시작할 수 없다. 병원에서 검사 장비가 켜지지 않았는데 환자의 건강 상태를 진단할 수 없는 것과 같다.

---

## 5단계. 관제와 로그의 차이

### 관제

관제는 밖에서 프로그램 상태를 계속 측정하는 일이다. 병실의 심박수 모니터와 비슷하다.

`monitor.sh`는 다음을 한 줄에 기록한다.

```text
시간, 프로세스 이름, PID, CPU, MEM, RSS, 스레드 수,
프로세스 상태, 포트 상태, 디스크 사용률, 시스템 메모리
```

### 애플리케이션 로그

로그는 프로그램이 자기 행동을 글로 남긴 기록이다. 환자가 “가슴이 아프다”고 말하거나 의료진이 처치 기록을 남기는 것과 비슷하다.

```text
[MemoryWorker] Current Heap: 50MB
[MemoryGuard] Memory limit exceeded
```

### 둘을 함께 봐야 하는 이유

관제는 객관적인 외부 상태를 보여주지만 프로그램의 의도까지 알기 어렵다. 앱 로그는 내부 행동을 설명하지만 기록 코드가 잘못되거나 마지막 순간에 저장되지 않을 수 있다.

따라서 다음처럼 서로 보완한다.

```text
외부 관제: RSS가 실제로 증가했다.
내부 로그: Heap을 25MB씩 늘렸고 제한에 도달했다.
종료 상태: exit 137, 즉 SIGKILL이다.
결론     : MemoryGuard가 메모리 증가를 감지해 자체 종료했다.
```

---

## 6단계. OOM과 메모리 누수 이해하기

### 6.1 메모리 누수란

프로그램이 더 이상 필요하지 않은 메모리를 계속 잡고 있어 재사용하지 못하게 하는 현상이다.

도서관에서 읽은 책을 반납하지 않고 자리 주변에 계속 쌓는 상황을 생각해 보자. 책이 남아 있어야 할 이유가 없지만 대출 상태가 유지되므로 다른 사람이 사용할 수 없다.

### 6.2 OOM이란

OOM은 Out Of Memory의 줄임말이다. 사용할 수 있는 메모리가 부족한 상태를 뜻한다.

실제 운영체제는 심각한 메모리 부족이 발생하면 OOM Killer로 프로세스를 강제 종료할 수 있다. 하지만 이번 Before 사례는 운영체제 OOM Killer가 아니라 앱 내부의 MemoryGuard가 설정된 제한값을 보고 먼저 종료했다.

### 6.3 실제 증거 읽기

```text
[MemoryWorker] Current Heap: 25MB
[MemoryWorker] Current Heap: 50MB
[MemoryGuard] Memory limit exceeded (50MB >= 50MB)
[MemoryGuard] Self-terminating process 2970
exit 137
```

한 줄씩 해석하면 다음과 같다.

1. 프로그램이 25MB의 Heap을 사용했다.
2. 시간이 지나 50MB로 증가했다.
3. 설정된 제한 50MB 이상이 됐다.
4. MemoryGuard가 PID 2970을 종료했다.
5. exit 137은 SIGKILL 종료와 일치한다.

### 6.4 Before & After가 알려주는 것

| 설정 | 생존 시간 | 결론 |
| --- | ---: | --- |
| 50MB | 약 6초 | 50MB 도달 후 종료 |
| 100MB | 약 12초 | 100MB 도달 후 종료 |

제한을 두 배로 올렸더니 생존 시간이 두 배로 늘었다. 이것은 메모리 증가 속도가 비슷하다는 추가 증거다.

그러나 100MB에서도 종료됐다. 창고를 두 배로 넓혔지만 물건을 계속 쌓는 습관을 고치지 않은 것과 같다.

### 6.5 임시 조치와 근본 해결

- 임시 조치: `MEMORY_LIMIT`을 높여 긴급 작업 시간을 확보한다.
- 근본 해결: 사용이 끝난 객체를 해제하고 캐시에 크기 제한과 만료 시간을 둔다.
- 검증 방법: 오랫동안 실행해도 RSS가 무한히 증가하지 않고 일정 범위로 돌아오는지 확인한다.

### 다른 사람에게 30초로 설명하기

> 이 프로그램은 실행 중 메모리를 25MB씩 계속 늘렸습니다. 외부 RSS도 16.8MB에서 41.8MB로 증가했습니다. 50MB 제한에서는 약 6초, 100MB 제한에서는 약 12초 후 MemoryGuard가 SIGKILL로 자체 종료했습니다. 제한을 올리면 시간을 벌 수 있지만 누수 자체는 남아 있으므로, 불필요한 객체나 무제한 캐시를 코드에서 정리해야 합니다.

---

## 7단계. CPU Spike 이해하기

### 7.1 CPU Spike란

CPU 사용률이 짧은 시간에 급격히 높아지는 현상이다. 순간적으로 높아졌다가 바로 내려오는 정상 동작일 수도 있지만, 높은 상태가 이어지면서 서비스 응답이 느려지면 장애가 된다.

톨게이트 직원 한 명이 복잡한 계산을 계속하느라 한 차량을 붙잡고 있으면 뒤의 차량이 밀리는 상황과 비슷하다.

### 7.2 실제 증거 읽기

```text
5.00 → 13.10 → 22.66 → 31.38 → 41.79 → 47.05 → 52.88%
[CRITICAL] CPU Threshold Violated!
exit 143
```

1. 내부 CpuWorker의 부하가 계속 증가했다.
2. 약 50%의 보호 기준을 넘었다.
3. 임계치 위반 로그가 남았다.
4. exit 143으로 종료됐다.
5. 143은 `128 + SIGTERM 번호 15`이므로 SIGTERM 종료와 일치한다.

### 7.3 왜 ps 숫자는 낮았는가

실습에서 앱 내부 관제값은 52.88%까지 올라갔지만 `ps` 표본은 약 0.3~1.0%로 낮았다.

이 차이를 숨기면 안 된다. 가능한 이유는 다음과 같다.

- `ps %CPU`가 실행 이후 평균을 표시한다.
- 측정 시점 사이의 짧은 변화를 놓칠 수 있다.
- VM의 CPU 할당과 앱 내부 측정 기준이 다를 수 있다.
- 런처와 실제 워커가 별도 프로세스다.

따라서 CPU 장애 주장은 내부 연속값, 임계치 로그, 종료 신호를 묶어서 제시했다. 운영 환경에서는 `pidstat`, `top -H`, cgroup CPU 통계처럼 더 촘촘한 외부 측정도 추가해야 한다.

### 7.4 Before & After

| 설정 | 동작 | 결과 |
| --- | --- | --- |
| 100% | 52.88%까지 계속 상승 | 약 24초 뒤 보호 종료 |
| 10% | 10% 도달 후 5%까지 냉각 | 90초 동안 자체 종료 없음 |

10% 설정에서 나타난 로그는 다음 뜻이다.

```text
Peak reached 10%  → 설정한 꼭대기에 도달
Starting cooldown → 부하를 내리기 시작
Cooldown complete 5% → 안전한 수준까지 내려옴
Resuming           → 다시 작업 시작
```

### 7.5 임시 조치와 근본 해결

- 임시 조치: `CPU_MAX_OCCUPY`를 낮춘다.
- 장점: 다른 프로세스가 CPU를 사용할 여유가 생긴다.
- 단점: 해당 프로그램의 처리 속도가 느려질 수 있다.
- 근본 해결: 불필요한 반복 계산을 제거하고 작업을 나누며 요청 속도를 제한한다.

### 다른 사람에게 30초로 설명하기

> CPU 설정을 100%로 두자 앱 내부 부하가 5%에서 52.88%까지 계속 상승했고, 보호 임계치를 넘은 직후 SIGTERM으로 종료됐습니다. 상한을 10%로 낮추자 5~10% 사이에서 냉각을 반복하며 살아 있었습니다. 제한을 낮추는 것은 임시 조치이고, 계산 구조와 반복 작업을 개선하는 것이 근본 해결입니다.

---

## 8단계. Deadlock 이해하기

### 8.1 교착상태란

두 개 이상의 작업이 서로 상대가 가진 자원을 기다려 영원히 진행하지 못하는 상태다.

두 사람이 좁은 골목 양쪽에서 마주 보고 “당신이 먼저 비켜야 내가 간다”고 기다리는 상황과 비슷하다. 둘 다 살아 있고 움직일 능력도 있지만 조건이 풀리지 않아 진행하지 못한다.

### 8.2 실제 락 관계

```text
Thread-1: Shared_Memory_A를 보유하고 Socket_Pool_B를 기다림
Thread-2: Socket_Pool_B를 보유하고 Shared_Memory_A를 기다림
```

그림으로 표현하면 다음과 같다.

```text
Thread-1 ──보유──> A
Thread-1 ──대기──> B

Thread-2 ──보유──> B
Thread-2 ──대기──> A

Thread-1 → B → Thread-2 → A → Thread-1
```

기다림이 원 모양으로 연결되어 누구도 먼저 끝낼 수 없다.

### 8.3 교착상태의 네 조건

| 조건 | 쉬운 뜻 | 이번 사례 |
| --- | --- | --- |
| 상호 배제 | 한 번에 한 명만 사용 | A와 B는 한 스레드만 보유 |
| 점유 대기 | 하나를 가진 채 다른 것을 기다림 | 각 스레드가 락 하나를 보유 |
| 비선점 | 남의 자원을 강제로 빼앗지 못함 | 상대 락이 풀릴 때까지 대기 |
| 순환 대기 | 기다림이 원을 만듦 | Thread-1→B→Thread-2→A |

네 조건 중 하나라도 없애면 Deadlock을 예방할 수 있다.

### 8.4 외부 증거 읽기

```text
PID  LWP   CPU  MEM  STAT WCHAN
2846 2846  0.0  0.1  SNl  futex_
2846 2874  0.0  0.1  SNl  futex_
2846 2875  0.0  0.1  SNl  futex_
```

- PID 2846: 프로세스는 아직 존재한다.
- LWP: 프로세스 안의 개별 스레드 번호다.
- CPU 0.0: 현재 계산이 진행되지 않는다.
- WCHAN `futex_`: 락이 풀리기를 기다리며 잠든 상태다.
- 같은 상태가 반복: 잠깐 쉬는 것이 아니라 정체가 이어진다.

로그의 마지막 두 줄도 결정적인 증거다.

```text
Thread-2 WAITING for Shared_Memory_A (BLOCKED)
Thread-1 WAITING for Socket_Pool_B (BLOCKED)
```

### 8.5 왜 PID와 포트만 보면 안 되는가

Deadlock 상태에서는 프로세스가 종료되지 않는다. TCP 포트도 열려 있을 수 있다. 단순 관제는 이를 정상으로 판단할 수 있다.

```text
PID 있음        → 살아 있음
포트 LISTEN     → 연결을 받을 준비는 표시됨
작업 로그 정지  → 실제 업무 진행 안 됨
모든 스레드 대기 → 내부 처리 불가능
```

따라서 “살아 있음”과 “정상적으로 일함”을 구분해야 한다.

### 8.6 Before & After

| 설정 | 결과 |
| --- | --- |
| `MULTI_THREAD_ENABLE=true` | 두 스레드가 서로의 락을 기다리며 정지 |
| `MULTI_THREAD_ENABLE=false` | A, B, C 작업이 모두 완료 |

### 8.7 임시 조치와 근본 해결

- 임시 조치: 멀티스레드를 끄고 한 번에 하나씩 처리한다.
- 단점: 동시에 처리할 수 없어 성능이 떨어진다.
- 근본 해결: 모든 스레드가 A→B처럼 같은 순서로 락을 획득한다.
- 추가 방법: 중첩 락을 줄이고, 일정 시간이 지나면 락을 포기하고 다시 시도한다.

### 다른 사람에게 30초로 설명하기

> Thread-1은 A를 가진 채 B를 기다리고, Thread-2는 B를 가진 채 A를 기다렸습니다. PID와 포트는 살아 있었지만 세 스레드가 모두 CPU 0%와 futex 대기 상태였고 로그도 멈췄습니다. 멀티스레드를 끄자 작업이 완료됐지만 이는 임시 회피입니다. 근본적으로 모든 스레드가 락을 같은 순서로 잡게 해야 합니다.

---

## 9단계. Round-Robin 이해하기

### 9.1 스케줄링이란

처리할 작업이 여러 개일 때 누구에게 언제 CPU 사용 기회를 줄지 정하는 방법이다.

### 9.2 대표적인 세 방식

| 방식 | 쉬운 설명 |
| --- | --- |
| FCFS | 먼저 온 일을 끝까지 처리한 뒤 다음 일을 처리 |
| Priority | 중요도가 높은 일을 먼저 처리 |
| Round-Robin | 각 작업에 일정한 차례를 주고 순환 |

### 9.3 실제 로그

```text
A 20·40% → B 20·40% → C 20·40%
A 60·80% → B 60·80% → C 60·80%
A 100% → B 100% → C 100%
```

A가 끝나기 전에 B와 C가 실행됐으므로 FCFS가 아니다. 특정 작업만 먼저 처리되지 않았으므로 Priority의 증거도 없다. A→B→C 순서로 비슷한 작업량을 배분했으므로 애플리케이션 수준의 Round-Robin으로 추론한다.

주의할 점은 Linux 커널 전체의 스케줄러를 판정한 것이 아니라는 사실이다. 여기서는 앱 로그에 나타난 자체 Scheduler의 작업 배분 방식만 분석했다.

---

## 10단계. 종료 코드와 신호 이해하기

Linux에서는 프로세스에 신호를 보내 특정 행동을 요청할 수 있다.

| 신호 | 번호 | 뜻 |
| --- | ---: | --- |
| SIGTERM | 15 | 정리할 기회를 주는 일반 종료 요청 |
| SIGKILL | 9 | 즉시 강제 종료, 거부하거나 처리할 수 없음 |
| SIGINT | 2 | 터미널에서 `Ctrl+C`를 눌렀을 때 주로 전달 |

신호로 종료된 셸 프로세스는 흔히 `128 + 신호 번호` 형태의 종료 코드를 남긴다.

```text
137 = 128 + 9  → SIGKILL
143 = 128 + 15 → SIGTERM
```

단, 종료 코드만 보고 누가 신호를 보냈는지까지 확정할 수는 없다. 로그와 실행 상황을 함께 확인해야 한다.

이번 테스트에서는 다음처럼 판단했다.

- OOM Before/After: MemoryGuard가 자체 종료한다고 로그에 명시했고 exit 137이다.
- CPU Before: 임계치 위반 직후 exit 143이다.
- CPU After의 90초 종료: 시험자가 관찰을 마치고 보낸 종료이므로 장애 종료가 아니다.

---

## 11단계. 생성된 결과물 읽는 순서

파일을 다음 순서로 읽으면 가장 이해하기 쉽다.

### 1. README.md

프로젝트의 목차다. 어떤 결과물이 어디에 있는지 빠르게 파악한다.

### 2. 이 학습 자료

기초 용어와 세 장애의 원리를 먼저 이해한다.

### 3. 실행_테스트_기록.md

실제 명령, 출력, 해설을 시간순으로 확인한다. 이론이 실제 출력에서 어떻게 보이는지 연결한다.

### 4. issues 디렉터리의 세 보고서

현상→증거→원인→조치 및 검증의 GitHub Issue 구조를 익힌다.

```text
issues/01_oom_crash.md
issues/02_cpu_latency.md
issues/03_deadlock.md
```

### 5. B1-2_수행내역서.md

세 장애를 하나의 표와 기술적 해석으로 종합한다.

### 6. run-case.sh와 monitor.sh

문서를 이해한 뒤 실제 자동화 코드를 읽는다.

### 7. 발표_시연_가이드.md

다른 사람 앞에서 어떤 순서로 명령을 실행하고 무엇을 설명할지 연습한다.

---

## 12단계. run-case.sh 읽기

`run-case.sh`는 여섯 가지 실행 상황을 짧은 이름으로 선택하게 해 준다.

| 실행 이름 | 실제 설정 |
| --- | --- |
| `oom-before` | 메모리 50, CPU 100, 스레드 false |
| `oom-after` | 메모리 100, CPU 100, 스레드 false |
| `cpu-before` | 메모리 512, CPU 100, 스레드 false |
| `cpu-after` | 메모리 512, CPU 10, 스레드 false |
| `deadlock-before` | 메모리 512, CPU 10, 스레드 true |
| `deadlock-after` | 메모리 512, CPU 10, 스레드 false |

### 스크립트의 흐름

```text
사용자가 사례 이름 입력
   ↓
case 문으로 환경변수 선택
   ↓
uname -m으로 CPU 아키텍처 확인
   ↓
ARM64 또는 x86 바이너리 선택
   ↓
필수 디렉터리와 secret.key 준비
   ↓
환경변수 export
   ↓
exec로 앱 실행
```

### 알아야 할 셸 문법

```bash
case "$case_name" in
```

여러 선택지 중 입력값과 맞는 경우를 고르는 문법이다. 문과적인 비유로는 신청서의 선택 항목에 따라 다른 안내문을 주는 창구와 같다.

```bash
export MEMORY_LIMIT="$memory_limit"
```

현재 셸에서 실행할 앱이 환경변수를 읽을 수 있게 공개한다.

```bash
exec "$app_binary"
```

셸 프로세스 자리를 실제 앱으로 교체한다. 종료 신호와 종료 코드가 앱 실행과 자연스럽게 연결된다.

---

## 13단계. monitor.sh 읽기

`monitor.sh 1 10`은 1초 간격으로 10번 측정하라는 뜻이다.

### 스크립트의 흐름

```text
측정 주기와 횟수 검사
   ↓
필요한 Linux 명령 존재 확인
   ↓
이름이 일치하는 프로세스 목록 검색
   ↓
RSS가 가장 큰 PID를 실제 워커로 선택
   ↓
CPU, MEM, RSS, 스레드, 상태 수집
   ↓
15034 포트와 시스템 자원 확인
   ↓
화면과 monitor.log에 동시에 기록
```

### 왜 RSS가 가장 큰 프로세스를 선택하는가

제공 앱은 PyInstaller 방식으로 포장되어 작은 런처 프로세스와 실제 작업 프로세스가 함께 보일 수 있다. 첫 측정에서는 작은 런처의 RSS만 약 1.7MB로 표시되어 메모리 증가를 놓쳤다.

수정한 `monitor.sh`는 같은 이름을 가진 프로세스 중 RSS가 가장 큰 PID를 고른다. 그 결과 실제 워커의 16.8→41.8MB 변화를 기록할 수 있었다.

이 사례가 주는 교훈은 “명령이 실행됐는가”보다 “올바른 대상을 측정했는가”가 더 중요하다는 것이다.

---

## 14단계. GitHub Issue 형식으로 보고하는 법

세 보고서는 같은 네 부분으로 구성됐다.

### 1. Description

무슨 일이, 어떤 조건에서 발생했는지 적는다.

나쁜 예:

```text
앱이 이상하다.
```

좋은 예:

```text
MEMORY_LIMIT=50으로 실행한 뒤 Heap이 50MB에 도달하자
약 6초 만에 프로세스가 exit 137로 종료됐다.
```

### 2. Evidence & Logs

주장을 뒷받침하는 숫자, 시간, PID, 명령 출력, 핵심 로그를 기록한다. 너무 긴 전체 로그보다 결론과 직접 연결되는 구간을 선택한다.

### 3. Root Cause Analysis

증거가 왜 그 원인을 가리키는지 논리적으로 연결한다.

```text
RSS와 Heap 상승
   +
제한 도달 로그
   +
MemoryGuard 자체 종료 로그
   +
exit 137
   =
메모리 증가를 감지한 보호 종료
```

### 4. Workaround & Verification

무엇을 바꿨고 결과가 어떻게 달라졌는지 Before & After로 비교한다. 임시 조치와 근본 해결을 구분한다.

---

## 15단계. 장애 세 가지 비교하기

| 질문 | OOM | CPU Spike | Deadlock |
| --- | --- | --- | --- |
| 프로세스가 종료되는가? | 예 | 예 | 아니오 |
| 주로 변하는 값 | RSS, Heap 증가 | CPU 내부 부하 증가 | CPU·메모리 정체 |
| 마지막 핵심 로그 | Memory limit exceeded | CPU Threshold Violated | WAITING/BLOCKED |
| OS 상태 증거 | RSS 상승 | 프로세스별 CPU 관제 | 스레드 futex 대기 |
| 종료 코드 | 137 | 143 | 없음, 수동 종료 |
| 임시 조치 | 메모리 제한 상향 | CPU 상한 하향 | 멀티스레드 비활성화 |
| 근본 해결 | 메모리 해제 | 계산 구조 개선 | 락 순서 통일 |

### 빠른 판단 질문

```text
프로세스가 사라졌는가?
 ├─ 예 → 종료 직전 메모리가 증가했는가?
 │       ├─ 예 → OOM/MemoryGuard 의심
 │       └─ 아니오 → CPU 임계치 로그가 있는가?
 │                  └─ 예 → CPU Watchdog 의심
 └─ 아니오 → 로그와 자원 변화가 멈췄는가?
         └─ 예 → 스레드 futex와 락 대기 확인 → Deadlock 의심
```

이 판단표는 출발점일 뿐 실제 운영 장애를 자동으로 확정하는 공식은 아니다. 반드시 로그와 여러 관제값을 함께 확인해야 한다.

---

## 16단계. 비전공자에게 설명할 때 지킬 원칙

### 원칙 1. 용어보다 현상을 먼저 말한다

처음부터 “futex 기반 mutex 경합”이라고 말하지 않는다.

```text
두 작업이 서로 가진 열쇠를 기다려 아무도 진행하지 못했습니다.
Linux에서는 이 대기 상태가 futex로 관측됐습니다.
```

### 원칙 2. 숫자는 비교해서 말한다

```text
메모리가 높았습니다.                  (약함)
RSS가 16.8MB에서 41.8MB로 늘었습니다. (좋음)
```

### 원칙 3. 관측과 추론을 구분한다

```text
관측: 로그에 CPU 52.88%와 Threshold Violated가 기록됐다.
추론: 내부 보호 정책이 과점유를 막기 위해 SIGTERM을 보냈다.
```

### 원칙 4. 임시 조치와 해결을 구분한다

```text
메모리 제한 상향 → 더 오래 생존, 누수는 남음
누수 코드 수정   → 근본 해결
```

### 원칙 5. 모르는 부분을 숨기지 않는다

CPU 사례에서 내부 값과 `ps` 값이 달랐다는 사실을 밝히고 측정 방식의 한계를 설명한다. 정확한 보고서는 모든 숫자가 완벽하게 일치하는 보고서가 아니라, 차이와 한계까지 기록한 보고서다.

---

## 17단계. 스스로 설명하는 연습

### 1분 설명 예시

> 이번 과제는 프로그램이 느려지거나 멈췄을 때 재부팅부터 하지 않고 증거로 원인을 찾는 실습입니다. OOM 사례에서는 RSS와 Heap이 계속 증가한 뒤 MemoryGuard가 프로세스를 종료했습니다. CPU 사례에서는 내부 부하가 52.88%까지 올라 보호 정책이 SIGTERM으로 종료했습니다. Deadlock 사례에서는 프로세스와 포트는 살아 있었지만 두 스레드가 서로의 락을 기다리고 모든 스레드가 futex에서 멈췄습니다. 환경변수를 바꿔 각 문제를 완화한 뒤 Before & After를 비교했으며, 제한 변경은 임시 조치이고 코드 구조 수정이 근본 해결이라는 결론을 내렸습니다.

### 설명 연습 질문

다음 질문에 문서를 보지 않고 답해 본다.

1. 프로세스가 살아 있다는 사실만으로 서비스 정상이라고 할 수 없는 이유는?
2. Heap과 RSS는 왜 같은 숫자가 아니어도 되는가?
3. OOM 테스트에서 50MB와 100MB를 비교해 무엇을 알았는가?
4. exit 137과 exit 143은 각각 어떤 신호와 관련 있는가?
5. Deadlock의 네 조건은 무엇인가?
6. 멀티스레드를 끄는 조치가 근본 해결이 아닌 이유는?
7. CPU 내부 값과 `ps` 값이 다를 때 어떻게 보고해야 하는가?
8. Round-Robin이라고 추론한 로그 패턴은 무엇인가?

---

## 18단계. 확인 문제

### 문제 1

프로세스의 PID와 포트는 존재하지만 10분 동안 로그가 추가되지 않고 모든 스레드가 `futex`에서 대기한다. 가장 먼저 의심할 장애는 무엇인가?

### 문제 2

메모리 제한을 100MB에서 200MB로 올렸더니 종료 시간이 두 배가 됐지만 결국 같은 로그로 종료됐다. 이 변경은 근본 해결인가?

### 문제 3

`CPU Threshold Violated` 직후 종료 코드 143이 기록됐다. 어떤 종료 신호와 관련 있는가?

### 문제 4

Thread-A가 끝나기 전에 B와 C가 실행되고, A→B→C 순서가 반복된다. FCFS, Priority, Round-Robin 중 무엇으로 추론할 수 있는가?

### 문제 5

관제 스크립트가 이름이 같은 두 프로세스 중 RSS 1.7MB인 런처만 측정했다. 실제 워커는 RSS 40MB를 사용 중이다. 이 관제 결과를 그대로 믿어도 되는가?

### 정답과 해설

1. Deadlock을 의심한다. PID 존재와 서비스 정상은 다르며, 로그 정지와 여러 스레드의 지속적인 락 대기가 핵심 증거다.
2. 아니다. 생존 시간만 늘고 같은 장애가 재발했으므로 임시 완화다.
3. SIGTERM이다. 일반적으로 143은 128+15로 해석한다.
4. Round-Robin이다. 작업 완료 전 다른 작업에 차례를 주고 순환한다.
5. 믿으면 안 된다. 올바른 PID를 선택하지 않았으므로 실제 메모리 변화를 놓친 측정이다.

---

## 19단계. 직접 실습하는 권장 순서

한 번에 세 장애를 모두 실행하기보다 다음 순서로 연습한다.

### 첫 번째 연습: 출력 읽기

1. `oom-before`를 실행한다.
2. Heap 증가 문장을 소리 내어 읽는다.
3. `monitor.sh`에서 RSS, PID, PORT를 찾는다.
4. 종료 코드 137의 뜻을 설명한다.

### 두 번째 연습: 비교하기

1. `oom-before`와 `oom-after`의 시간을 기록한다.
2. 달라진 환경변수 하나를 찾는다.
3. 왜 근본 해결이 아닌지 한 문장으로 적는다.

### 세 번째 연습: 측정 한계 찾기

1. `cpu-before`의 내부 값을 기록한다.
2. 같은 시점의 `monitor.sh` 값을 기록한다.
3. 두 값이 다른 이유와 추가로 필요한 도구를 설명한다.

### 네 번째 연습: 멈춤 진단하기

1. `deadlock-before`를 실행한다.
2. PID와 포트를 확인한다.
3. 마지막 BLOCKED 로그를 찾는다.
4. `ps -L`의 `futex`를 확인한다.
5. 네 가지 교착 조건을 실제 로그에 대응시킨다.

### 다섯 번째 연습: 발표하기

`발표_시연_가이드.md`를 보면서 10분 안에 세 장애를 설명한다. 두 번째 연습부터는 문장을 그대로 읽지 말고 자신의 표현으로 바꾸어 말한다.

---

## 20단계. 최종 핵심 정리

이번 문제의 핵심은 명령어 암기가 아니라 다음 사고 순서다.

```text
1. 정상 실행 조건을 확인한다.
2. 장애를 재현한다.
3. 시간에 따른 변화를 관제한다.
4. 마지막 로그와 프로세스 상태를 수집한다.
5. 관측 사실과 원인 추론을 구분한다.
6. 설정을 하나만 바꾼다.
7. Before & After를 같은 기준으로 비교한다.
8. 임시 조치와 근본 해결을 구분한다.
9. 다른 사람이 재현할 수 있게 기록한다.
```

가장 중요한 문장은 다음과 같다.

> 프로세스가 종료됐다는 결과만 보지 말고, 종료 직전까지 CPU·메모리·스레드·로그가 어떻게 변했는지를 증거로 설명해야 한다.

이 원칙을 이해하면 OOM, CPU Spike, Deadlock뿐 아니라 다른 서버 장애도 같은 방식으로 접근할 수 있다.
