# B1-2 발표 시연 가이드

## 1. 시연 목표

`agent-leak-app`의 OOM, CPU Spike, Deadlock을 사람들 앞에서 직접 재현하고, 관제 결과를 근거로 원인과 조치 전후를 설명한다. 전체 시연 시간은 약 8~10분이다.

## 2. 발표 전 준비

### 준비물

- OrbStack이 실행 중인 Mac
- 미리 생성한 Ubuntu VM `b1-troubleshooting`
- B1-2 프로젝트 디렉터리
- 터미널 창 2개

### 프로젝트 디렉터리 이동

두 터미널 모두 Mac에서 프로젝트 디렉터리로 이동한 뒤 VM에 접속한다.

```bash
cd "/Users/jingeollee/Desktop/학습/Codyssey_Study_Files/B1-2/codyssey2026-B1-2"
orb -m b1-troubleshooting bash
```

OrbStack VM은 Mac 디렉터리를 같은 경로에 마운트하므로 VM 접속 후에도 프로젝트 파일을 바로 사용할 수 있다.

### 실행 권한과 파일 확인

```bash
chmod +x run-case.sh monitor.sh
ls -l run-case.sh monitor.sh agent-leak-app-arm64
```

### 이전 프로세스 정리

이전 연습에서 남은 프로세스가 있으면 15034번 포트가 충돌할 수 있다.

```bash
pgrep -af agent-leak-app
```

결과가 있다면 해당 앱만 종료한다.

```bash
pkill -f agent-leak-app
```

다음 명령에서 아무 결과가 없으면 시연 준비가 끝난 것이다.

```bash
ss -ltnp | grep ':15034' || echo '15034 port is ready'
```

## 3. 터미널 배치

| 터미널 | 용도 | 주로 실행할 명령 |
| --- | --- | --- |
| 터미널 A | 애플리케이션 실행과 핵심 로그 표시 | `./run-case.sh ...` |
| 터미널 B | 프로세스, 메모리, 포트, 스레드 관제 | `./monitor.sh`, `ps -L` |

두 창을 좌우로 배치하고 글자 크기를 미리 키워 둔다. 터미널 A에서 앱을 먼저 시작한 직후 터미널 B에서 관제를 시작한다.

## 4. OOM/MemoryGuard 시연

### 4.1 Before: 메모리 제한 50MB

터미널 A:

```bash
./run-case.sh oom-before
```

터미널 B:

```bash
./monitor.sh 1 8
```

약 6초 동안 다음 내용을 확인한다.

- RSS가 약 16.8MB에서 41.8MB로 증가한다.
- 내부 Heap이 25MB에서 50MB로 증가한다.
- TCP 15034는 종료 직전까지 LISTEN 상태다.
- `Memory limit exceeded`와 `Self-terminating`이 출력된다.
- 프로세스가 exit 137로 종료된다.

발표 해설:

> 메모리 사용량이 시간에 따라 상승하고 제한값 50MB에 도달했습니다. 네트워크 장애나 Linux OOM Killer가 아니라 애플리케이션의 MemoryGuard가 시스템 보호를 위해 SIGKILL로 자체 종료한 상황입니다. 종료 코드 137은 128에 SIGKILL 번호 9를 더한 값입니다.

### 4.2 After: 메모리 제한 100MB

터미널 A:

```bash
./run-case.sh oom-after
```

터미널 B:

```bash
./monitor.sh 1 14
```

이번에는 Heap이 25→50→75→100MB로 증가하고 약 12초 뒤 종료된다.

발표 해설:

> 제한을 두 배로 올리자 생존 시간도 약 6초에서 12초로 늘었습니다. 하지만 같은 증가 패턴과 종료가 반복되므로 제한 상향은 시간을 버는 임시 조치일 뿐입니다. 근본적으로는 불필요한 객체 참조와 무제한 캐시를 제거해야 합니다.

## 5. CPU Spike/Watchdog 시연

### 5.1 Before: CPU 상한 100%

터미널 A:

```bash
./run-case.sh cpu-before
```

터미널 B:

```bash
./monitor.sh 1 30
```

약 24초 동안 내부 CPU 관제값이 다음과 같이 상승한다.

```text
5.00 → 13.10 → 22.66 → 31.38 → 41.79 → 47.05 → 52.88%
```

`CPU Threshold Violated!`가 출력된 직후 프로세스가 exit 143으로 종료된다.

발표 해설:

> 특정 CpuWorker의 부하가 계속 상승하여 내부 보호 기준인 약 50%를 넘었습니다. 종료 코드 143은 SIGTERM 번호 15가 더해진 값이므로 Watchdog 성격의 보호 종료와 일치합니다.

외부 `ps` 값이 내부 로그보다 낮게 보이는 경우에는 다음과 같이 설명한다.

> `ps %CPU`는 프로세스가 시작된 이후의 평균값이므로 짧은 피크를 낮게 표시할 수 있습니다. 여기서는 연속 내부 관제값, 임계치 위반 로그, SIGTERM 종료를 함께 판단했습니다.

### 5.2 After: CPU 상한 10%

터미널 A:

```bash
./run-case.sh cpu-after
```

터미널 B:

```bash
./monitor.sh 1 30
```

다음 로그가 반복되는지 확인한다.

```text
Peak reached (10.00%). Starting cooldown...
Cooldown complete (5.00%). Resuming load increase...
```

약 30초 동안 관찰한 뒤 터미널 A에서 `Ctrl+C`로 종료한다.

발표 해설:

> CPU 상한을 10%로 낮추자 부하가 5~10% 사이에서 증가와 냉각을 반복하고 Watchdog 종료가 발생하지 않았습니다. 안정성을 높였지만 처리 성능이 감소할 수 있으므로 계산 구조 개선이 근본 해결책입니다.

> 이 실행은 시험자가 `Ctrl+C`로 종료한 것이다. 자체 Watchdog 종료와 혼동하지 않는다.

## 6. Deadlock 시연

### 6.1 Before: 멀티스레드 활성화

터미널 A:

```bash
./run-case.sh deadlock-before
```

약 10초 뒤 다음 관계를 나타내는 로그에서 출력이 멈춘다.

```text
Thread-1: Shared_Memory_A 보유 → Socket_Pool_B 대기
Thread-2: Socket_Pool_B 보유 → Shared_Memory_A 대기
```

터미널 B에서 PID가 유지되는지 확인한다.

```bash
pgrep -af agent-leak-app
./monitor.sh 1 5
```

가장 최근에 생성된 실제 워커 PID를 선택하고 스레드 상태를 조회한다.

```bash
worker_pid="$(pgrep -n -f agent-leak-app)"
echo "worker PID: $worker_pid"
ps -L -p "$worker_pid" -o pid,lwp,pcpu,pmem,stat,wchan,comm
```

예상되는 핵심 상태는 세 스레드의 `CPU 0.0`과 `WCHAN futex_`이다.

발표 해설:

> Thread-1과 Thread-2가 각각 하나의 락을 보유한 채 상대 락을 기다리고 있습니다. PID와 포트는 살아 있지만 모든 스레드가 futex에서 잠들어 서비스는 무응답입니다. 이는 상호 배제, 점유 대기, 비선점, 순환 대기의 네 조건이 모두 성립하는 교착상태입니다.

관찰이 끝나면 터미널 A에서 `Ctrl+C`로 종료한다.

### 6.2 After: 멀티스레드 비활성화

터미널 A:

```bash
./run-case.sh deadlock-after
```

A→B→C 순서로 작업이 반복되고 다음 로그가 출력되는지 확인한다.

```text
[Scheduler] All tasks completed.
```

확인 후 `Ctrl+C`로 종료한다.

발표 해설:

> 멀티스레드를 끄자 순환 락 대기가 사라지고 모든 작업이 완료됐습니다. 다만 이것은 처리량을 포기한 회피책입니다. 근본 해결은 모든 스레드가 락을 같은 순서로 획득하게 만드는 것입니다.

## 7. 보너스: 스케줄링 설명

정상 실행 로그의 작업 순서는 다음과 같다.

```text
A 20·40% → B 20·40% → C 20·40%
A 60·80% → B 60·80% → C 60·80%
A 100% → B 100% → C 100%
```

발표 해설:

> A가 끝나기 전에 B와 C가 실행되므로 FCFS가 아닙니다. 특정 작업의 독점이나 우선 실행도 없습니다. 같은 작업량을 A, B, C에 차례로 배분하고 Preempted와 Resumed가 반복되므로 애플리케이션 수준의 Round-Robin으로 추론할 수 있습니다.

## 8. 발표 순서 요약표

| 순서 | 장애 | Before | 관찰 시간 | After | 결정적 증거 |
| ---: | --- | --- | ---: | --- | --- |
| 1 | OOM | 50MB | 약 6초 | 100MB | RSS·Heap 상승, exit 137 |
| 2 | CPU | 100% | 약 24초 | 10% | 52.88%, exit 143, cooldown |
| 3 | Deadlock | Thread true | 약 10초 | false | PID 유지, BLOCKED, futex |
| 4 | Bonus | 정상 로그 | 약 1초 | - | A→B→C 순환 실행 |

## 9. 문제가 생겼을 때 복구

### Port 15034 is not available

이전 앱이 남아 있는지 확인하고 해당 앱만 종료한다.

```bash
pgrep -af agent-leak-app
pkill -f agent-leak-app
```

### VM이 보이지 않거나 접속되지 않음

OrbStack 앱을 실행한 뒤 VM 목록을 확인한다.

```bash
orb list
```

`b1-troubleshooting`이 없으면 발표 전에 VM을 다시 준비해야 한다.

### Permission denied

```bash
chmod +x run-case.sh monitor.sh agent-leak-app-arm64
```

### Deadlock에서 로그가 멈춤

의도된 결과다. PID와 `futex` 대기를 보여준 뒤 터미널 A에서 `Ctrl+C`로 종료한다.

### monitor.sh가 not-running을 출력함

앱이 이미 종료됐거나 터미널 B의 관제가 늦게 시작된 것이다. 앱을 먼저 실행하고 1초 안에 관제를 시작한다.

## 10. 발표 전 최종 체크리스트

- [ ] OrbStack과 `b1-troubleshooting` VM이 실행된다.
- [ ] 터미널 두 개가 같은 프로젝트 디렉터리에 있다.
- [ ] `run-case.sh`, `monitor.sh`, ARM64 바이너리에 실행 권한이 있다.
- [ ] 15034번 포트를 사용하는 이전 프로세스가 없다.
- [ ] 각 Before 사례의 예상 시간을 숙지했다.
- [ ] After 사례의 수동 종료를 자체 종료로 설명하지 않는다.
- [ ] CPU의 내부 관제값과 `ps` 평균값 차이를 설명할 수 있다.
- [ ] Deadlock에서 실제 워커 PID를 선택할 수 있다.
- [ ] 상세 수치가 필요하면 `실행_테스트_기록.md`를 열어 둔다.
