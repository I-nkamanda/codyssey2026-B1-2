# B1-2 수행내역서

## 1. 미션 개요

`agent-leak-app`에서 발생하는 OOM Crash, CPU Latency, Deadlock을 Linux 표준 도구와 애플리케이션 로그로 식별하고, 환경변수 조정 전후를 비교하였다. 분석은 바이너리 내부를 보지 않고 실행 중 외부에서 관측한 증거만 사용했다.

## 2. 실습 환경과 부트 조건

| 항목 | 실측/설정값 |
| --- | --- |
| 격리 환경 | OrbStack Ubuntu 22.04 VM |
| 커널/아키텍처 | Linux 7.0.14-orbstack, aarch64 |
| 실행 계정 | 일반 사용자 `jingeollee` (uid 501) |
| 실행 파일 | `agent-leak-app-arm64` |
| `AGENT_PORT` | 15034 |
| `AGENT_HOME` | `/tmp/b1-2-agent` (실측 시) |
| 필수 하위 경로 | `upload_files`, `api_keys`, `logs` |
| 키 파일 | `api_keys/secret.key`, 권한 제한 |

환경변수 없이 최초 실행했을 때 `[2/6] Verifying Environment Variables [FAIL]` 및 `Critical Env 'AGENT_HOME' is missing`가 출력됐다. 필수 디렉터리와 키를 만든 뒤에는 부트 검사 6단계가 모두 `[OK]`, 이어서 `Agent READY`가 확인됐다.

재현 도구 `run-case.sh`는 CPU 아키텍처에 맞는 바이너리를 선택하고 사례별 환경변수를 구성한다. `monitor.sh`는 가장 큰 RSS를 사용하는 실제 워커 PID를 선택하므로 PyInstaller 런처 프로세스만 잘못 측정하는 문제를 피한다.

## 3. 장애별 결론

| Case | Before | 관측 결과 | After | 검증 결과 |
| --- | --- | --- | --- | --- |
| OOM | `MEMORY_LIMIT=50` | Heap 25→50MB, 약 6초, exit 137 | `MEMORY_LIMIT=100` | Heap 25→100MB, 약 12초, 생존 시간 약 2배 |
| CPU | `CPU_MAX_OCCUPY=100` | 내부 부하 5→52.88%, 임계치 위반, exit 143 | `CPU_MAX_OCCUPY=10` | 90초 동안 생존, 5~10% 범위에서 냉각 반복 |
| Deadlock | `MULTI_THREAD_ENABLE=true` | PID 유지, 3개 스레드 0% CPU/futex 대기, 로그 정지 | `false` | A→B→C 순환 실행 후 `All tasks completed` |

세부 증거와 원인 분석은 각 GitHub Issue 형식 문서에 기록했다.

## 4. 공통 관제 절차

앱을 실행한 터미널과 별개의 터미널에서 다음을 수행한다.

```bash
./monitor.sh 1 10
pgrep -af agent-leak-app
ps -L -p <worker-pid> -o pid,lwp,pcpu,pmem,stat,wchan,comm
ss -ltnp | grep ':15034'
tail -f .agent-home/logs/agent_app.log
```

`ps`의 `%CPU`는 실행 이후의 평균값이므로 짧은 급상승을 낮게 표시할 수 있다. CPU 사례에서는 앱 내부 관제값과 종료 코드, 외부 프로세스 상태를 함께 판단했다. 반대로 Deadlock에서는 단일 순간이 아니라 여러 번의 샘플에서 CPU 0%, `futex` 대기, 로그 정지가 동시에 유지되는지를 확인했다.

## 5. 기술적 해석

### 메모리

애플리케이션이 힙 객체 참조를 계속 유지하면 가비지 컬렉터가 회수할 수 없고 RSS가 증가한다. 실측에서 워커 RSS는 약 17MB에서 43MB로 증가했고 앱의 논리 Heap은 25MB에서 50MB로 증가했다. 종료는 Linux OOM Killer가 아니라 애플리케이션의 MemoryGuard가 스스로 SIGKILL을 보낸 결과다. 따라서 제한 상향은 시간을 벌 뿐 누수 자체를 해결하지 않는다.

### CPU

특정 프로세스의 계산 루프가 CPU 시간을 계속 사용하면 같은 호스트의 다른 프로세스가 실행 대기열에서 더 오래 기다려 지연이 커진다. 이 앱은 내부 부하가 보호 임계치를 넘자 종료되었다. 제한을 낮추면 부하 상승과 냉각을 반복하여 급격한 과점유를 피하지만, 실제 수정에서는 계산 루프 최적화, 작업 분할, rate limiting, cgroup quota를 검토해야 한다.

### Deadlock

Thread-1은 A를 보유한 채 B를, Thread-2는 B를 보유한 채 A를 기다렸다. 상호 배제, 점유 대기, 비선점, 순환 대기의 네 조건이 모두 성립한다. `futex`는 사용자 공간 mutex가 경합할 때 커널에서 잠드는 대표적인 대기 지점이다. 멀티스레드를 끄는 것은 회피책이며, 근본 해결은 모든 스레드가 락을 같은 순서로 획득하도록 설계를 바꾸는 것이다.

## 6. 완료 기준 점검

- [x] OOM 메모리 상승과 종료 직전/직후 로그 확보
- [x] `MEMORY_LIMIT` 변경 전후 2회 실행 비교
- [x] CPU 상승 구간, 임계치 위반, SIGTERM 종료 확인
- [x] `CPU_MAX_OCCUPY` 변경 전후 비교
- [x] Deadlock PID 존재, 스레드 정체, 마지막 BLOCKED 로그 확보
- [x] `MULTI_THREAD_ENABLE` true/false 비교
- [x] GitHub Issue 형식의 기술 보고서 3건 작성
- [x] 보너스 스케줄링 알고리즘 추론 작성
