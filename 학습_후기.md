# 학습 후기

B1-1에서는 프로세스가 살아 있는지와 포트가 열렸는지를 확인하는 관제를 만들었다면, B1-2에서는 같은 관제값을 시간의 흐름으로 읽고 장애 원인을 설명하는 연습을 했다.

가장 인상적인 점은 “프로세스가 존재한다”가 “서비스가 정상이다”와 같지 않다는 사실이다. Deadlock 사례는 PID와 포트가 그대로인데도 세 스레드가 모두 `futex`에서 잠들고 로그가 멈췄다. 앞으로는 단일 상태값보다 PID, CPU, 메모리, 스레드 대기 지점, 마지막 로그를 함께 확인해야 한다.

또한 환경변수 조정은 대부분 임시 조치였다. 메모리 제한을 두 배로 늘리면 생존 시간도 늘었지만 결국 같은 이유로 종료됐다. CPU 제한을 낮추거나 멀티스레드를 끄는 것도 장애를 피할 수는 있으나 처리 성능과 기능을 줄인다. 좋은 리포트는 “일단 살리는 조치”와 “코드에서 고칠 원인”을 구분해야 한다는 점을 배웠다.

마지막으로 실제 관제 도구의 숫자도 맥락 없이 믿으면 안 된다는 점을 확인했다. `ps %CPU`는 실행 이후 평균이라 짧은 피크를 놓칠 수 있고, PyInstaller 런처만 선택하면 실제 워커의 RSS를 잘못 측정한다. 수집 주기와 PID 선택 기준까지 기록해야 다른 사람이 같은 결론을 재현할 수 있다.
