# B1-2 실행 테스트 기록

## 1. 기록 목적

이 문서는 `agent-leak-app`을 실제 Ubuntu VM에서 실행한 순서와 결과를 재현 가능한 형태로 정리한 기록이다. 단순히 성공·실패 결과만 나열하지 않고, 각 출력이 어떤 운영체제 상태와 장애 원인을 의미하는지 함께 설명한다.

테스트는 2026-09-06에 진행했으며, 과제에서 금지한 바이너리 디컴파일과 리버스 엔지니어링은 수행하지 않았다. 아래 분석은 프로그램 출력과 Linux 표준 명령으로 관측한 내용만을 근거로 한다.

## 2. 테스트 환경

```text
OS       : Ubuntu 22.04 (OrbStack VM)
Kernel   : Linux 7.0.14-orbstack-00380-ga7e0a2dc9535
CPU      : aarch64
User     : jingeollee, uid=501 (non-root)
Binary   : agent-leak-app-arm64
Port     : TCP 15034
Lab path : /tmp/b1-2-agent
```

ARM64 VM이므로 `file` 명령으로 `agent-leak-app-arm64`가 `ELF 64-bit ... ARM aarch64`인지 확인한 뒤 사용했다. root가 아닌 일반 계정으로 실행하여 첫 번째 부트 조건도 충족했다.

## 3. 최초 실행: 필수 환경변수 누락 확인

### 실행

```bash
./agent-leak-app-arm64
```

### 핵심 출력

```text
>>> Starting Agent Boot Sequence...
[1/6] Checking User Account               [OK]
   ... Running as service user 'jingeollee' (uid=501)
[2/6] Verifying Environment Variables     [FAIL]
   >>> Critical Env 'AGENT_HOME' is missing.
[3/6] Checking Required Files             [FAIL]
   >>> Skipped due to previous critical failure.
[4/6] Checking Port Availability          [FAIL]
   >>> Skipped due to previous critical failure.
[5/6] Verifying Log Permission            [FAIL]
   >>> Skipped due to previous critical failure.
[6/6] Verifying Mission Environment       [FAIL]
   >>> Skipped due to previous critical failure.
System Boot Failed. Process Terminated.
```

### 해설

실행 계정 검사는 통과했지만 `AGENT_HOME`이 없어서 다음 검사가 연쇄적으로 생략됐다. 프로그램 장애를 분석하기 전에 실행 전제 조건부터 충족해야 한다는 뜻이다. 이 실패는 OOM이나 CPU 장애가 아니라 설정 오류다.

## 4. 실행 환경 구성 및 정상 부트 확인

### 구성한 항목

```bash
mkdir -p /tmp/b1-2-agent/upload_files
mkdir -p /tmp/b1-2-agent/api_keys
mkdir -p /tmp/b1-2-agent/logs
printf '%s\n' 'agent_api_key_test' > /tmp/b1-2-agent/api_keys/secret.key
```

실행 시 다음 환경변수를 전달했다.

```text
AGENT_HOME=/tmp/b1-2-agent
AGENT_PORT=15034
AGENT_UPLOAD_DIR=/tmp/b1-2-agent/upload_files
AGENT_KEY_PATH=/tmp/b1-2-agent/api_keys
AGENT_LOG_DIR=/tmp/b1-2-agent/logs
```

### 핵심 출력

```text
[1/6] Checking User Account               [OK]
[2/6] Verifying Environment Variables     [OK]
[3/6] Checking Required Files             [OK]
[4/6] Checking Port Availability          [OK]
[5/6] Verifying Log Permission            [OK]
[6/6] Verifying Mission Environment       [OK]
------------------------------------------------------------
All Boot Checks Passed!
Agent READY
Agent listening at port 15034
```

### 해설

여섯 검사가 모두 통과했고 TCP 15034 바인딩도 성공했다. 이후 테스트에서 발생한 종료나 무응답은 부트 설정 실패와 구분할 수 있다.

## 5. OOM/MemoryGuard 테스트

### 5.1 Before: MEMORY_LIMIT=50

```bash
./run-case.sh oom-before
```

별도 터미널에서 다음과 같이 1초 간격으로 관제했다.

```bash
./monitor.sh 1 4
```

### monitor.sh 결과

```text
[2026-09-06 01:31:11] PROCESS:agent-leak-app PID:2970 CPU:1.0% MEM:0.1%
RSS:16.8MB THREADS:1 STATE:SN PORT:15034:LISTEN DISK_USED:1% SYSTEM_MEM:4.4%
[2026-09-06 01:31:12] PROCESS:agent-leak-app PID:2970 CPU:0.5% MEM:0.1%
RSS:16.8MB THREADS:1 STATE:SN PORT:15034:LISTEN DISK_USED:1% SYSTEM_MEM:4.4%
[2026-09-06 01:31:13] PROCESS:agent-leak-app PID:2970 CPU:0.6% MEM:0.3%
RSS:41.8MB THREADS:1 STATE:SN PORT:15034:LISTEN DISK_USED:1% SYSTEM_MEM:4.6%
[2026-09-06 01:31:14] PROCESS:agent-leak-app PID:2970 CPU:0.5% MEM:0.3%
RSS:41.8MB THREADS:1 STATE:SN PORT:15034:LISTEN DISK_USED:1% SYSTEM_MEM:4.6%
```

### 애플리케이션 종료 로그

```text
2026-09-06 01:31:12,394 [INFO] [MemoryWorker] Current Heap: 25MB
2026-09-06 01:31:15,420 [INFO] [MemoryWorker] Current Heap: 50MB
2026-09-06 01:31:15,420 [CRITICAL] [MemoryGuard] Memory limit exceeded (50MB >= 50MB)
2026-09-06 01:31:15,420 [CRITICAL] [MemoryGuard] Self-terminating process 2970 to prevent system instability.
run_case_exit=137
```

### 해설

워크 프로세스의 RSS가 16.8MB에서 41.8MB로 상승했고, 애플리케이션 내부 Heap도 25MB에서 50MB로 증가했다. 포트는 종료 직전까지 LISTEN 상태였으므로 네트워크 오류가 원인이 아니다.

종료 코드 137은 `128 + SIGKILL(9)`이다. 마지막 로그에 MemoryGuard가 스스로 종료한다고 명시되어 있으므로 Linux 커널의 OOM Killer가 임의로 죽인 상황과 구분된다.

### 5.2 After: MEMORY_LIMIT=100

```bash
./run-case.sh oom-after
```

### 핵심 출력

```text
[MemoryWorker] Current Heap: 25MB
[MemoryWorker] Current Heap: 50MB
[MemoryWorker] Current Heap: 75MB
[MemoryWorker] Current Heap: 100MB
[CRITICAL] [MemoryGuard] Memory limit exceeded (100MB >= 100MB)
[CRITICAL] [MemoryGuard] Self-terminating process 1797 to prevent system instability.
exit_status=137 elapsed_seconds=12
```

### 비교 해설

| 항목 | 50MB | 100MB |
| --- | ---: | ---: |
| 종료 시간 | 약 6초 | 약 12초 |
| 관측된 Heap 단계 | 25, 50 | 25, 50, 75, 100 |
| 종료 신호 | SIGKILL | SIGKILL |

제한을 두 배로 늘리자 생존 시간도 약 두 배가 됐지만 결국 같은 원인으로 종료됐다. 따라서 제한 상향은 임시 완화책이며, 메모리 참조 해제나 캐시 상한 적용이 근본 해결책이다.

## 6. CPU Spike/Watchdog 테스트

### 6.1 Before: CPU_MAX_OCCUPY=100

```bash
./run-case.sh cpu-before
```

### 애플리케이션 관제 로그

```text
01:22:33 [CpuWorker] Current Load: 5.00%
01:22:36 [CpuWorker] Current Load: 13.10%
01:22:39 [CpuWorker] Current Load: 22.66%
01:22:42 [CpuWorker] Current Load: 31.38%
01:22:45 [CpuWorker] Current Load: 36.93%
01:22:48 [CpuWorker] Current Load: 41.79%
01:22:51 [CpuWorker] Current Load: 47.05%
01:22:55 [CpuWorker] Current Load: 52.88%
01:22:55 [CRITICAL] [CpuWorker] CPU Threshold Violated! (52.879999999999995%).
launcher_exit=143 elapsed_seconds=24
```

### ps 표본

```text
01:22:42 PID 2641 CPU 0.5% MEM 0.1% RSS 17208KB STAT SN
01:22:51 PID 2641 CPU 0.8% MEM 0.1% RSS 17208KB STAT SN
01:22:53 PID 2641 CPU 1.0% MEM 0.1% RSS 17208KB STAT SN
```

### 해설

애플리케이션 내부의 연속 관제값은 5%에서 52.88%까지 증가했고, 보호 임계치 위반 직후 종료됐다. 종료 코드 143은 `128 + SIGTERM(15)`이므로 Watchdog 성격의 보호 종료와 일치한다.

한편 `ps %CPU`는 프로세스가 시작된 뒤의 평균 사용률이며 짧은 부하 변화를 평탄화할 수 있다. 이 VM에서는 내부 관제값과 외부 평균값의 차이가 컸다. 따라서 하나의 순간값만 선택하지 않고 연속 내부 로그, 임계치 위반 메시지, SIGTERM 종료를 함께 증거로 사용했다. 실제 운영 서버에서는 `pidstat -p <pid> 1`이나 cgroup CPU 통계도 병행하는 것이 좋다.

### 6.2 After: CPU_MAX_OCCUPY=10

```bash
./run-case.sh cpu-after
```

### 핵심 출력

```text
[CpuWorker] Current Load: 5.00%
[CpuWorker] Peak reached (10.00%). Starting cooldown...
[CpuWorker] Current Load: 8.36%
[CpuWorker] Current Load: 6.70%
[CpuWorker] Cooldown complete (5.00%). Resuming load increase...
```

### 비교 해설

100% 설정에서는 약 24초 뒤 내부 부하 52.88%에서 종료됐지만, 10% 설정에서는 5~10% 범위에서 증가와 냉각을 반복하며 90초 동안 생존했다. 90초 뒤의 종료 코드 143은 관찰을 끝내기 위해 시험자가 보낸 SIGTERM이며 자체 Watchdog 종료가 아니다.

CPU 상한을 낮추는 방법은 시스템 안정성을 높이는 임시 조치다. 근본적으로는 busy loop 제거, 작업 분할, rate limiting, blocking I/O 적용 등을 검토해야 한다.

## 7. Deadlock 테스트

### 7.1 Before: MULTI_THREAD_ENABLE=true

```bash
./run-case.sh deadlock-before
```

### 마지막 애플리케이션 로그

```text
[Worker-Thread-1] LOCK ACQUIRED: [Shared_Memory_A]. (Holding...)
[Worker-Thread-2] LOCK ACQUIRED: [Socket_Pool_B]. (Holding...)
[AgentWorker] Waiting for worker threads to complete transactions...
[Worker-Thread-2] Need resource [Shared_Memory_A] to write logs.
[Worker-Thread-2] WAITING for [Shared_Memory_A]... (Status: BLOCKED)
[Worker-Thread-1] Need resource [Socket_Pool_B] to finish job.
[Worker-Thread-1] WAITING for [Socket_Pool_B]... (Status: BLOCKED)
```

### ps -L 스레드 관측

```text
PID  LWP   CPU  MEM  STAT WCHAN  COMMAND
2846 2846  0.0  0.1  SNl  futex_ agent-leak-app
2846 2874  0.0  0.1  SNl  futex_ agent-leak-app
2846 2875  0.0  0.1  SNl  futex_ agent-leak-app
```

이 출력은 여러 번의 샘플에서 동일하게 반복됐다. 25초 후에도 PID 2846은 살아 있었지만 추가 애플리케이션 로그가 없어서 시험자가 종료했다.

### 해설

Thread-1은 Shared_Memory_A를 가진 채 Socket_Pool_B를 기다리고, Thread-2는 Socket_Pool_B를 가진 채 Shared_Memory_A를 기다렸다. 서로 상대가 가진 자원을 기다리므로 순환 대기가 형성됐다.

세 스레드가 모두 0% CPU와 `futex` 대기 상태인 점은 계산을 수행하는 중이 아니라 mutex가 풀리기를 기다리며 잠든 상태임을 보여준다. PID와 포트가 존재하는지만 확인했다면 정상으로 오판할 수 있는 사례다.

### 7.2 After: MULTI_THREAD_ENABLE=false

```bash
./run-case.sh deadlock-after
```

### 핵심 출력

```text
Thread-A 20% → 40% → Preempted
Thread-B 20% → 40% → Preempted
Thread-C 20% → 40% → Preempted
Thread-A 60% → 80% → Preempted
Thread-B 60% → 80% → Preempted
Thread-C 60% → 80% → Preempted
Thread-A 100% → Thread-B 100% → Thread-C 100%
[Scheduler] All tasks completed.
```

### 비교 해설

멀티스레드를 끄자 순환 락 대기가 발생하지 않았고 세 작업이 모두 완료됐다. 이는 장애가 네트워크나 포트 문제가 아니라 동시 실행 시의 락 획득 순서에서 발생한다는 판단을 강화한다.

다만 동시 실행 비활성화는 처리량을 포기하는 회피책이다. 실제 코드에서는 모든 스레드가 자원을 같은 순서로 잠그게 만들고, 중첩 락을 줄이며, 필요한 경우 timeout과 rollback을 도입해야 한다.

## 8. 정상 실행 로그를 통한 스케줄링 추론

정상 실행에서는 A→B→C가 각각 40%까지 처리된 뒤 같은 순서로 80%, 100%까지 진행됐다. 하나의 작업이 끝날 때까지 실행하는 FCFS가 아니며, 특정 스레드가 독점하는 Priority 패턴도 아니다. 같은 작업량을 배분하고 `Preempted`와 `Resumed`가 반복되므로 애플리케이션 수준의 Round-Robin 스케줄링으로 추론했다.

이 결론은 Linux 커널 전체의 스케줄링 정책이 Round-Robin이라는 의미가 아니다. 관측한 애플리케이션 Scheduler의 작업 배분 방식에 한정한 결론이다.

## 9. 최종 스크립트 연동 검증

작성한 `run-case.sh`와 `monitor.sh`를 함께 실행하여 다음을 확인했다.

- VM 아키텍처에 맞는 ARM64 바이너리 자동 선택
- 필요한 디렉터리와 `secret.key` 자동 준비
- 사례별 환경변수 적용
- PyInstaller 런처가 아닌 실제 워커 PID 선택
- RSS 16.8MB→41.8MB 증가 기록
- TCP 15034 LISTEN 기록
- MemoryGuard 종료와 exit 137 확인

두 스크립트는 `bash -n` 문법 검사를 통과했으며, 문서의 링크와 Git 공백 오류 검사도 통과했다.

## 10. 테스트 결론

| 장애 | 결정적 증거 | 임시 조치 | 결과 |
| --- | --- | --- | --- |
| OOM | RSS·Heap 상승, MemoryGuard, exit 137 | 메모리 제한 50→100MB | 생존 6→12초, 재발 |
| CPU | 내부 부하 5→52.88%, 임계치 위반, exit 143 | CPU 상한 100→10% | 90초 동안 자체 종료 없음 |
| Deadlock | PID 유지, 두 BLOCKED 로그, 3개 futex 대기 | 멀티스레드 true→false | 모든 작업 완료 |

세 장애 모두 환경변수 변경으로 현상을 완화하거나 회피할 수 있었다. 그러나 메모리 해제, CPU 작업 구조 개선, 일관된 락 순서 적용이 각각의 근본 해결책이다.
